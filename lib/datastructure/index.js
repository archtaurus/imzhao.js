const Stack = require('./stack')
const LinkedList = require('./linkedlist')
const { Queue, PriorityQueue } = require('./queue')
require('./set')

module.exports = {
    Stack,
    Queue,
    PriorityQueue,
    LinkedList,
}
