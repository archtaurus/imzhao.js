class Node {
    constructor(x, y) {
        this.x = x
        this.y = y
        this.visited = false
        this.neighbors = []
    }
}

describe('astar', () => {
    test('b', () => {})
})
